// Mock/demo data has been removed. The app only ever displays real data
// fetched from the FastAPI backend — on failure, API routes now return the
// real error instead of falling back to fabricated sample data. This file is
// kept only because the sandbox filesystem does not allow deleting it;
// nothing in the app imports it.
export {};
