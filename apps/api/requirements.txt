# CPU-only torch build (~180MB) instead of the default CUDA build pip would
# otherwise resolve as sentence-transformers' transitive dependency (~770MB,
# with bundled CUDA/cuDNN libraries mapped into memory at import time). On
# Render's free tier (512MB RAM total for the whole process), the CUDA build
# was almost certainly what OOM-killed the container on first search/embed
# call. Must come before sentence-transformers so pip resolves this pin
# instead of pulling the unconstrained default.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0
pgvector==0.3.6
redis==5.2.1
celery==5.4.0
requests==2.32.3
feedparser==6.0.11
python-dateutil==2.9.0.post0
numpy==2.2.1
scikit-learn==1.6.0
networkx==3.4.2
python-louvain==0.16
structlog==24.4.0
sentence-transformers==3.3.1
tenacity==9.0.0
httpx==0.28.1
